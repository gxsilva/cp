#include <bits/stdc++.h>

using namespace std;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	int x;
	long long y;
	char z;
	float a;
	double b;

	cin >> x >> y >> z >> a >> b;
	cout << x << '\n' << y << '\n' << z << '\n' << a << '\n' << b << '\n';
	return (0);
}