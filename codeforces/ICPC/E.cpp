#include <bits/stdc++.h>

using namespace std;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout << fixed << setprecision(9);

	double pi = 3.141592653;
	double r;
	cin >> r;
	cout << (pow(r, 2)) * pi << '\n';
	return (0); 
}